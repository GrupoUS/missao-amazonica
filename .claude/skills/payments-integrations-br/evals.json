{
  "skill_name": "payments-integrations-br",
  "evals": [
    {
      "id": 1,
      "prompt": "Implement the Asaas webhook handler for payment events. It needs to handle PAYMENT_RECEIVED and PAYMENT_OVERDUE. Make sure we don't process the same event twice.",
      "expected_output": "Handler that stores event.id in DB with UNIQUE constraint (onConflictDoNothing), returns 200 within 10s, processes async. Uses AbortSignal.timeout. Reads API key from env config module.",
      "assertions": [
        {
          "id": "A01",
          "description": "Uses event.id as idempotency key with UNIQUE constraint or onConflictDoNothing",
          "check": "contains: onConflictDoNothing OR UNIQUE",
          "critical": true
        },
        {
          "id": "A02",
          "description": "Responds 200 before async processing (not after)",
          "check": "contains: async",
          "critical": true
        },
        {
          "id": "A03",
          "description": "Handles PAYMENT_RECEIVED and PAYMENT_OVERDUE event types",
          "check": "contains: PAYMENT_RECEIVED",
          "critical": false
        },
        {
          "id": "A04",
          "description": "Does NOT hardcode API key",
          "check": "not-contains: Bearer sk_",
          "critical": true
        }
      ]
    },
    {
      "id": 2,
      "prompt": "We're getting 401s from the Kiwify API. How do we handle token expiry correctly?",
      "expected_output": "Explanation of 24h JWT token, pre-emptive refresh at ~23h, inline 401 detection → re-auth → retry once pattern. Mentions POST /v1/oauth/token endpoint.",
      "assertions": [
        {
          "id": "B01",
          "description": "Mentions 24-hour token lifetime",
          "check": "contains: 24",
          "critical": true
        },
        {
          "id": "B02",
          "description": "Describes inline 401 → re-auth → retry pattern",
          "check": "contains: 401",
          "critical": true
        },
        {
          "id": "B03",
          "description": "References /v1/oauth/token endpoint",
          "check": "contains: oauth/token",
          "critical": false
        }
      ]
    },
    {
      "id": 3,
      "prompt": "Emit an NFe via Nuvem Fiscal. What do I need to be careful about?",
      "expected_output": "Validate CNPJ/CPF before sending. Store chaveAcesso immediately. Check for existing chaveAcesso to prevent duplicate emission. Distinguish hard SEFAZ rejections (fix data, no retry) from soft errors (retry with backoff). Use separate tokens for sandbox/prod.",
      "assertions": [
        {
          "id": "C01",
          "description": "Mentions storing chaveAcesso immediately",
          "check": "contains: chaveAcesso",
          "critical": true
        },
        {
          "id": "C02",
          "description": "Warns about duplicate emission / idempotency check",
          "check": "contains: duplicate OR idempoten",
          "critical": true
        },
        {
          "id": "C03",
          "description": "Distinguishes hard vs soft SEFAZ errors",
          "check": "contains: rejeic OR rejection OR retry",
          "critical": true
        },
        {
          "id": "C04",
          "description": "Mentions separate sandbox/production tokens or environments",
          "check": "contains: sandbox OR homologac",
          "critical": false
        }
      ]
    },
    {
      "id": 4,
      "prompt": "Set up a Hubla webhook receiver for purchase events.",
      "expected_output": "Implements HMAC-SHA256 verification with timingSafeEqual (noting this is assumed pending official docs). Uses event.id for idempotency. Notes that exact header name and algorithm should be verified from Hubla dashboard.",
      "assertions": [
        {
          "id": "D01",
          "description": "Implements or recommends HMAC-SHA256 signature verification",
          "check": "contains: HMAC OR sha256 OR timingSafeEqual",
          "critical": true
        },
        {
          "id": "D02",
          "description": "Notes Hubla docs require dashboard access / assumed pattern",
          "check": "contains: dashboard OR assumed OR verify",
          "critical": false
        },
        {
          "id": "D03",
          "description": "Includes idempotency guard",
          "check": "contains: idempoten OR onConflict OR UNIQUE",
          "critical": true
        }
      ]
    }
  ]
}
